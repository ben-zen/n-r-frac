[constants]
arch = 'riscv64-linux-gnu'

[binaries]
c = arch + '-gcc'
cpp = arch + '-g++'
ar = arch + '-ar'
strip = arch + '-strip'

# figuring out an exe wrapper situation here
exe_wrapper = '/usr/bin/qemu-riscv64 -L /usr/riscv64-linux-gnu'

[properties]
sys_root = '/usr/' + arch
pkg_config_libdir = '/usr/' + arch + '/lib/pkgconfig'

[host_machine]
system = 'linux'
kernel = 'linux'
cpu_family = 'riscv64'
cpu = 'rva23u64'
endian = 'little'
